import { Inter, Permanent_Marker, Roboto } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });
export const permanent_marker = Permanent_Marker({
  subsets: ["latin"],
  weight: ["400"],
});
export const roboto = Roboto({
  subsets: ["latin"],
  weight: ["400"],
});
